# Updates (2026-03-03) - Fidelity Validator

## Summary
- Fixed alpha sweep plotting to create the output directory before saving the figure.

## Details
- The alpha sweep visualization now calls `os.makedirs(save_dir, exist_ok=True)` so saving to `../data` works even if the folder is missing.

## What Was Changed
- Added a directory creation guard before saving `alpha_sweep_results.png` to prevent `FileNotFoundError`.

## 30% Fooling Rate Note
- The 30% fooling rate was reached after updating the training objective (negative-prompt loss + relaxed fooling threshold) and allowing slightly higher perturbation strength (`xi`, `alpha`) in the generator.

## Files Changed
- [python/fidelity_validator.py](python/fidelity_validator.py)
